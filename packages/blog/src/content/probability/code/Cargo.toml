[package]
name = "code"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
rand.workspace = true

getrandom = { version = "0.3.4", features = ["wasm_js"] }
wasm-bindgen = "0.2.106"

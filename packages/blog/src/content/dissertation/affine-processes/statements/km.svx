---
type: statement
kind: theorem
slug: keller-ressel-mayerhofer
---

<script>
  import Proof from '$lib/components/proof.svelte';
</script>

1. There exists a map $\aff: \momentSpace \times \stateSpace \rightarrow \bbR$ of the form
   $$
     \aff(t,\momentVar,\stateVar) = \affPart_0(t,\momentVar) + \bprj{\affPart(t,\momentVar)}{\stateVar}
   $$
   such that, for each $(\tau,\momentVar) \in \momentSpace$, $\aff(\cdot,\momentVar,\cdot)$ is a solution to $\system(\ode,\tau,\momentVar)$ dominated by all other such solutions.
   Moreover, this map satisfies the following for each $(\tau, \momentVar) \in \momentSpace$ and $\stateVar \in \stateSpace$.
   $$
     @tag(km-moments)
     \Exp_{\Prb_\stateVar}\exp\prj{\momentVar}{\X_t} = \exp\aff(t, \momentVar, \stateVar), \quad t \in [0,\tau]
   $$
2. If $\tau \geq 0$, $\momentVar \in \vecSpace$, and $\stateVar \in \stateSpace^\circ$ are such that  $\Exp_{\Prb_\stateVar}\exp\prj{\momentVar}{\X_\tau} < \infty$, then $(\tau, \momentVar) \in \momentSpace$.

<Proof>

With [%full](statement:riccati-affine-formulation), this is the same as [Theorem 2.14](cite:keller2015).

</Proof>
